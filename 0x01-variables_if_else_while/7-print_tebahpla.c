#include <stdio.h>
/**
 * main - main func
 * Description: func
 * Return: 0
 */
int main(void)
{
	char c = 'z';
	char p = '\n';

	for (c = 'z'; c >= 'a'; c++)
		putchar(c);
	putchar(p);

	return (0);
}
